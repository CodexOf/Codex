# Пример файла настроек для PostgreSQL
# Скопируйте этот файл в .env и заполните реальными данными

# URL подключения к базе данных PostgreSQL
# Получите его на Render.com после создания базы данных
DATABASE_URL=postgresql://username:password@host:port/database

# Режим работы (production для Render)
NODE_ENV=production

# Порт (Render устанавливает автоматически)
# PORT=3000
